[package]
name = "raven-engine"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
raven-core = { path = "../../lib/raven-core/" }
anyhow = "1.0.0"  # error lib
log = "0.4"       # logging lib
winit = "0.27.0"  # cross-platform window creation library