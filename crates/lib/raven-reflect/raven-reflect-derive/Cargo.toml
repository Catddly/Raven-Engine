[package]
name = "raven-reflect-derive"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
anyhow = "1.0.0"  # error lib

syn = { version = "1.0", features = ["full"] }
proc-macro2 = "1.0"
quote = "1.0"

bit-set = "0.5.3"

toml = "0.5.10"